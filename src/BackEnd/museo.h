#ifndef MUSEO_H
#define MUSEO_H

// Obtiene el precio para un museo específico
float getPrecioMuseo(int idMuseo);

// Obtiene las restricciones para un museo específico
const char* getRestriccionesMuseo(int idMuseo);

#endif // MUSEO_H